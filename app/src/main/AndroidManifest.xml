<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        MVP — ZERO permessi runtime.
        INTERNET non è un permesso runtime: non richiede dialog all'utente.
        Tutto il resto viene aggiunto solo quando strettamente necessario e sempre opt-in.
    -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!--
        Permessi futuri — commentati e pronti, da decommentare versione per versione:

        Versione 2.0 — barcode scan via Google Code Scanner (NON richiede CAMERA):
        nessun permesso aggiuntivo necessario.

        Versione 2.0 — upload foto etichette via Android Photo Picker:
        nessun permesso aggiuntivo necessario (READ_MEDIA_IMAGES non serve con Photo Picker).

        Versione 2.0 — notifiche alert prezzo (opt-in esplicito utente):
        <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

        Versione 3.0 — posizione foreground opzionale per "negozi vicino a me":
        <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
        <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

        MAI aggiungere:
        <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    -->

    <application
        android:name=".SavioApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Savio"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Savio">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
